Main Application: main.py

Dependencies: 
PyQt6
textual

Application flags
1. -d [working directory] 
Example: main.py -d ~/Downloads

2. --gui
Opens the same in gui, without this flag it will default to a tui app. 

Python_Edition contains all files present in Python Version